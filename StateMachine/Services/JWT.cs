using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.Claims;
using System.Threading.Tasks;


namespace StateMachine.Services
{
    public class JWT
    {
        /****
        public static Task<ClaimsIdentity> CreateClaimIdAsync(Models.User user)
        {
            ClaimsIdentity claimsIdentity = new ClaimsIdentity();
            claimsIdentity.AddClaim(new Claim(ClaimTypes.NameIdentifier, user.username.ToString()));

           

        }
         * ****/

    }
}
